
1.SELECT one.emp_no, last_name, first_name , gender 
From employees AS one JOIN 
salaries AS two ON one.emp_no = two.emp_no;

--select info
SELECT emp_no, last_name, first_name, gender, salary FROM two


2.SELECT first_name, last_name, hire_date FROM employees 
WHERE hire_date = '1986%';

3. SELECT departments.dept_no, departments.dept_name, dept_manager.emp_no, employees.last_name, employees.first_name, dept_manager.from_date, dept_manager.to_date
FROM departments
JOIN dept_manager
ON departments.dept_no = dept_manager.dept_no
JOIN employees
ON dept_manager.emp_no = employees.emp_no;
